# 构建中间产物
.build/
.build-*/
.cache/
build/
dist/
*.apk
*.dex
*.class

# tools/ 下测试套件跑出来的临时工作目录（跑完即删，不入库）
work/
hot/
tx-test/
payload.bin

# 编辑器 / 系统
.idea/
.vscode/
.DS_Store

# BGM 构建产物：由 tools/convert-bgm.py 从 assets/resource/sound_native/bgm/*.hca
# 在构建期生成（CI 里那一步）。源文件已在库里，产物没必要再占 3MB。
assets/cnv/bgm*.ogg
assets/cnv/bgm.json

# native 构建中间产物（magia-native 由 CI 编译，产物拷进 lib/<abi>/）
magia-native/build/
